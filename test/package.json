{
  "name": "bedrock-authn-token-test",
  "version": "0.0.1-0",
  "private": true,
  "scripts": {
    "test": "node --preserve-symlinks test.js test",
    "coverage": "cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text-summary npm test",
    "coverage-ci": "cross-env NODE_ENV=test nyc --reporter=text-lcov npm test > coverage.lcov",
    "coverage-report": "nyc report"
  },
  "dependencies": {
    "bedrock": "^3.0.0",
    "bedrock-account": "digitalbazaar/bedrock-account#mongo-version-3",
    "bedrock-authn-token": "file:..",
    "bedrock-identity": "digitalbazaar/bedrock-identity#mongo-version-3",
    "bedrock-mongodb": "^7.0.0",
    "bedrock-permission": "digitalbazaar/bedrock-permission#mongo-version-3",
    "bedrock-test": "^5.0.0",
    "bedrock-validation": "^4.2.0",
    "cross-env": "^7.0.2",
    "nyc": "^15.0.1",
    "otplib": "^12.0.1"
  },
  "nyc": {
    "excludeNodeModules": false,
    "include": [
      "node_modules/bedrock-authn-token/**"
    ],
    "exclude": [
      "node_modules/bedrock-authn-token/node_modules/**"
    ]
  }
}
